import {
  Box,
  Button,
  TextField,
  Paper,
  Divider,
  Dialog,
  DialogTitle,
  DialogContent,
  DialogActions,
  List,
  ListItem,
  // Autocomplete,
} from "@mui/material";

const Messages = ({ user, messages }) => {
  return (
    <Box>
      <List>
        <ListItem>Blah</ListItem>
      </List>
    </Box>
  );
};
